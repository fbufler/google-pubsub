version: "2"

linters:
  enable:
    - errcheck
    - govet
    - staticcheck

linters-settings:
  errcheck:
    check-blank: true

issues:
  exclude-dirs:
    - gen/
  exclude-rules:
    - path: "_test\\.go"
      linters:
        - errcheck
